name: Figure
status: experimental
group: "Atoms"

props:
  type: object
  properties:

    width:
      type: string
      enum:
        - 'width-80'
        - 'width-40'
        - 'width-30'
      default: 'width-40'

slots:
  image:
    title: Image
  caption:
    title: Caption
