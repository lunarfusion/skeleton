name: Tabs
description: "Tabs for local tasks"
group: "Navigation"
status: experimental
links:
   - "https://github.com/lunarfusion/skeleton"

props:
  type: object

  properties:
    position:
      type: string
      title: position
      description: 'Tabs positioning'
      default: relative
      enum:
        - relative
        - absolute


slots:
  items:
    title: "Tabs items"
