{%
  set classes = [
    'accordion',
    variant and variant != 'default' ? 'accordion--' ~ variant,
  ]
%}

<div{{ attributes.addClass(classes) }}>
  {% if items %}
   {{ items }}
  {% endif %}
</div>
