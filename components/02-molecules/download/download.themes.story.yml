name: On Primary Theme

props:
  variant: default

slots:
  file:
    - type: component
      component: "skeleton:file"
      slots:
        label: "download_file"
        description: "A description of the file to be downloaded"
        size: "24kb"
        url: "#"

library_wrapper: >
  <div class="theme-primary">
    {{ _story }}
  </div>
