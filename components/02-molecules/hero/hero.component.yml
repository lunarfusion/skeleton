name: Hero
description: Hero
group: "Molecules"
status: experimental
links:
   - "https://github.com/lunarfusion/skeleton"

# variants:
#   default:
#     title: Default

props:
  type: object
  properties:

    theme:
      title: Theme
      type: string
      description: 'Color theme for the hero.'
      enum:
        - default
        - primary
        - secondary
      default: default

  # items:
  #   $ref: ui-patterns://links
  #   title: Items
  #
  # disabled:
  #   title: Xx cannot be selected
  #   type: boolean
  #
  # readOnly:
  #   title: Xx is functionally disabled but still needs to be shown
  #   type: boolean
  #
  # icon:
  #   $ref: ui-patterns://icon
  #   title: Icon
  #   description: 'Xx icon'

slots:
  label:
    title: Label
    description: 'Label text of the Xx'

libraryOverrides:
    css:
      theme:
        xx.css: {}
