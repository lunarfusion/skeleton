{%
  set classes = [
    'hero',
    variant and variant != 'default' ? 'hero--' ~ variant,
    layout ? 'hero--' ~ layout,
    text_alignment and text_alignment != 'default' ? 'hero--' ~ text_alignment,
    height and height != 'default' ? 'hero--' ~ height,
  ]
%}



<div{{attributes.addClass(classes)}}>
  <div class="hero__content">
    <div class="hero__content-inner">

      {% if heading %}
        <div class="hero__heading">
          {{ heading }}
        </div>
      {% endif %}

      {# TODO - reevaluate empty render array issue 3425818 #}
      {% if text|render|trim %}
        <div class="hero__text">
          {{ text }}
        </div>
      {% endif %}
    </div>
  </div>

  {# TODO - reevaluate empty render array issue 3425818 #}
  {% if image|render|trim %}
    <div class="hero__image">
      {{ image }}
    </div>
  {% endif %}
</div>
