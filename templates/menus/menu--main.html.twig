{% if items %}
  {{ include ('skeleton:nav', {
    layout: 'vertical',
    collapsible: true,
    items: items,
    main: true,
    menu_id: 'main-menu',
    menu_name: 'main menu'
  }, with_context: false) }}
{% endif %}
