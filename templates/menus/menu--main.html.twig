{% if items %}
  {{ include ('skeleton:nav', {
    layout: 'vertical',
    collapsible: true,
    items: items,
    main: true,
    nav_id: 'main-nav',
    menu_name: 'main menu'
  }, with_context: false) }}
{% endif %}
