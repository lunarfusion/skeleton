{#
/**
* Referenced in Card Group Block
* Pulls cards items from Card Paragraph
 */
#}

  {# {{ item.content }} #}

{# TODO - field template is the wrong place for a component #}
{{ include ('skeleton:card_group', {
   cards: items,
 }, with_context: false) }}
