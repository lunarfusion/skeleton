{% if link_path -%}
  {{ include ('skeleton:author', {
    content: name,
    is_link: true,
    url: link_path,
  }, with_context: false) }}
{%- else -%}
  {{ include ('skeleton:author', {
    content: name,
    is_link: false,
  }, with_context: false) }}
{%- endif -%}
